import Link from "next/link";
import { requireAdmin } from "@/lib/admin/auth";
import { getAdminTrip } from "@/lib/admin/data";
import { createPlace, deletePlace, movePlace, savePlace } from "@/lib/admin/actions";
import { ConfirmSubmit } from "@/components/admin/ConfirmSubmit";

const categories = ["religious", "memorial", "monument", "palace", "hotel", "activity", "transfer"];

export default async function AdminPlacesPage({ params }: { params: Promise<{ tripId: string }> }) {
  const { client } = await requireAdmin(); const { tripId } = await params; const trip = await getAdminTrip(client, tripId); if (!trip) return null;
  return <section className="admin-section"><div className="admin-page-heading"><div><span className="section-label">{trip.title}</span><h1>Lugares</h1><p>{trip.places.length} fichas conectadas al relato.</p></div></div><form action={createPlace} className="admin-editor-card admin-create-card"><input type="hidden" name="tripSlug" value={trip.slug} /><div className="editor-card-heading"><strong>Nuevo lugar</strong></div><div className="admin-form-grid"><label>Nombre<input name="name" required /></label><label>Slug<input name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" placeholder="se-genera-desde-el-nombre" /></label><label>Ciudad<input name="city" required /></label><label>Zona<input name="zone" /></label><label>Categoria<select name="category" defaultValue="activity">{categories.map((category) => <option value={category} key={category}>{category}</option>)}</select></label><label>Fecha de visita<input type="date" name="visitDate" /></label><label className="field-wide">Resumen<input name="summary" /></label><label className="field-wide">Descripcion<textarea name="description" rows={3} /></label></div><fieldset><legend>Jornadas vinculadas</legend><div className="admin-check-grid">{trip.days.map((day) => <label key={day.id}><input type="checkbox" name="dayId" value={day.id} />Dia {day.dayNumber} · {day.title}</label>)}</div></fieldset><button className="admin-primary-link" type="submit">Crear lugar</button></form><div className="admin-editor-stack">{trip.places.map((place, index) => <article className="admin-editor-card" key={place.id}><div className="editor-card-heading"><span className="section-label">{place.category}</span><strong>{place.slug}</strong><div className="admin-card-actions"><form action={movePlace}><input type="hidden" name="tripSlug" value={trip.slug} /><input type="hidden" name="placeId" value={place.id} /><input type="hidden" name="direction" value="up" /><button type="submit" disabled={index === 0}>Subir</button></form><form action={movePlace}><input type="hidden" name="tripSlug" value={trip.slug} /><input type="hidden" name="placeId" value={place.id} /><input type="hidden" name="direction" value="down" /><button type="submit" disabled={index === trip.places.length - 1}>Bajar</button></form><Link href={`/viajes/${trip.slug}/lugares/${place.slug}`} target="_blank">Vista publica</Link></div></div><form action={savePlace}><input type="hidden" name="tripSlug" value={trip.slug} /><input type="hidden" name="placeId" value={place.id} /><div className="admin-form-grid"><label>Nombre<input name="name" defaultValue={place.name} required /></label><label>Slug<input name="slug" defaultValue={place.slug} required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" /></label><label>Nombre alternativo<input name="alternateName" defaultValue={place.alternateName} /></label><label>Ciudad<input name="city" defaultValue={place.city} required /></label><label>Zona<input name="zone" defaultValue={place.zone} /></label><label>Categoria<select name="category" defaultValue={place.category}>{categories.map((category) => <option value={category} key={category}>{category}</option>)}</select></label><label>Fecha de visita<input type="date" name="visitDate" defaultValue={place.visitDate} /></label><label>Latitud<input type="number" step="any" name="latitude" defaultValue={place.latitude ?? ""} /></label><label>Longitud<input type="number" step="any" name="longitude" defaultValue={place.longitude ?? ""} /></label><label className="field-wide">Resumen<input name="summary" defaultValue={place.summary} /></label><label className="field-wide">Descripcion<textarea name="description" defaultValue={place.description} rows={4} /></label></div><fieldset><legend>Jornadas vinculadas</legend><div className="admin-check-grid">{trip.days.map((day) => <label key={day.id}><input type="checkbox" name="dayId" value={day.id} defaultChecked={place.dayIds.includes(day.id)} />Dia {day.dayNumber} · {day.title}</label>)}</div></fieldset><button type="submit" className="admin-primary-link">Guardar lugar</button></form><form action={deletePlace}><input type="hidden" name="tripSlug" value={trip.slug} /><input type="hidden" name="placeId" value={place.id} /><ConfirmSubmit className="admin-danger-link" message="Eliminar este lugar y sus relaciones?">Eliminar lugar</ConfirmSubmit></form></article>)}</div></section>;
}
